%h1 
  Add a step for
  = recipe.name

= form_for [recipe, step] do |f|
  = f.label :description
  = f.text_area :description
  %br
  = f.label :picture
  = f.file_field :picture

  = f.submit "submit"
