%h1 
  Add a step for
  = recipe.name

= form_for [recipe, step] do |f|
  = f.label :description
  = f.text_area :description, class: 'form-control'
  %br
  = f.label :picture
  = f.file_field :picture
  %br
  = f.submit "submit"
